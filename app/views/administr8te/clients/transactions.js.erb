$('<div id="client_transactions"></div>')
		.html('<%= escape_javascript render(:partial => 'transactions') %>')
		.dialog({
			autoOpen: true,
			modal: true,
			title: '<%= @user.full_name %>',
			show: "fade",
			hide: "fade",
			width: 420
    });