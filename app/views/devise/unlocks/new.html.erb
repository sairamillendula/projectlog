<h1>Resend unlock instructions</h1>

<%= form_for(resource, :as => resource_name, :url => unlock_path(resource_name), :html => { :method => :post }) do |f| %>
  <%= devise_error_messages! %>

  <h2><%= f.label :email %></h2>
  <%= f.email_field :email %>

  <p class="submit"><%= f.submit "Resend unlock instructions" %></p>
<% end %>