<%= render :partial => 'submenu' %>

<div id="project-title">
   <h1><%= @project.internal? ? "#{@project.title}" : "#{@project.title} - #{@project.customer.name}" %>
     <% if @project.internal? %><span class="status internal">INTERNAL</span> <% end %>
     <% if @project.status? %>
       <span class="status open">OPEN</span>
     <% else %>
       <span class="status closed">CLOSED</span> 
     <% end %>
   </h1>
</div>

<div id="meta">
<% if @project.internal? %>
  <% if @project.description.blank? %>
	  <%= render 'description' %>
  <% else %>
    <div id="project_description">
	   Description: <%= markdown(@project.description) %>
	</div>
  <% end %>
<% else %>
  <div class="column_left">
    <% if @project.description.blank? %>
	  <%= render 'description' %>
	<% else %>
	  <%= markdown(@project.description) %>
    <% end %>
  </div>

  <div class="column_right">
    <%= render 'column_right' %>
  </div>
<% end %>
</div>

<%= clear %>

<div id="activities">
  <%= render "activities/activities" %>
</div>