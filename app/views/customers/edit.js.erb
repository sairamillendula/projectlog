$(document).ready(function(){
	$('<div id="customer_dialog"></div>')
		.html('<%= escape_javascript render(:partial => 'quick_form') %>')
		.dialog({
			autoOpen: true,
			modal: true,
			title: 'Edit Customer',
			buttons: { 'Update Customer' : function(){ $(this).find("form").submit(); }, 'Cancel' : function(){ $(this).dialog("close"); } },
			show: "fade",
			hide: "fade",
			width: 420
	    });
});