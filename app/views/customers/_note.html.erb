<%= form_for(@customer, :url => customer_path, :method => :post) do |f| %>

  <div id="customer-note">
    <p id="description-intro">Note (<a href="#">Markdown accepted</a>) <span class="small submit"><%= f.submit "Save" %></span>
    <span style="height: 100px"><%= f.text_area :note, :id => "note" %></span>
  </div>

<% end %>