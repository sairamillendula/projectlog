<%= form_for([@activity, @current_user.activities.new]) do |f| %>

<div class="alert">Andrés, can you please add JS calendar for date field and fix the bug. I want to post AJAX notification and render form again</div>

<table>
  <tr>
    <th>Date</th>
    <th>Time</th>
    <th>Project</th>
    <th>Description</th>
    <th></th>
  </tr>
  <tr>
    <td><%= f.date_select :date %></td>
    <td><%= f.text_field :time, :size => 4 %></td>
    <td><%= collection_select(:project, :project_id, current_user.projects.open.all, :id, :title, options = {:prompt => "Select Project"} ) %></td>
    <td><%= f.text_field :description, :size => 65 %></td>
    <td><%= f.submit 'Log it' %></td>
  </tr>
</table>

<% end %>

<p class="blue">Blue Button</p>