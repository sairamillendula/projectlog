<% if current_user.projects.any? %>
  <%= render :partial => 'submenu' %>

  <div id="quick-activity">
    <%= render :partial => 'activities/quick' %>
  </div>
  
  <%= render :partial => 'last_week_pie_charts' unless !current_user.activities.any? %>
  
  <%= render :partial => 'dashboard/activities/recent' %>
  
<% else %>
  <h1 class="intro-title">Get Started with Projectlog</h1>
  <h2 class="intro-tagline">
    Follow these easy steps to discover all the benefits of your business tracking system. This introduction will help you get started so you can do what you like most while Projectlog ensures wheels keep turning.
  </h2>

  <%= render :partial => 'paypal' %>
<% end %>